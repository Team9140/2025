package org.team9140.frc2025.subsystems;

// make a subsystem
public class Intake {
    // needs 1 victorSPX motor

    // config a current limit

    // a canRange for detecting is a coral in or not
    // make a method to return a Trigger using yes/not is something there

    // commands for grab ball, score ball, score coral, intake coral, hold current game piece (see 2023 intake for example)
}
